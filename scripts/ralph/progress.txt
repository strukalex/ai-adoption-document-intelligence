# Ralph Progress Log
Started: 2026-02-14

## Codebase Patterns

### Project Structure
- Monorepo with apps/backend-services (NestJS), apps/temporal (Temporal.io), apps/frontend (React), apps/shared (Prisma)
- Backend tests: `.spec.ts` | Temporal tests: `.test.ts`
- No shared import path between backend and temporal - types must be duplicated

### Testing Patterns
- Backend: Jest with NestJS Test.createTestingModule
- Temporal: Plain Jest with ts-jest
- Always run tests after modifying code
- Pre-existing test failures may exist (Prisma type mismatches) - verify new failures

### Important Commands
- Prisma generation: `npm run db:generate` from apps/backend-services (writes to both apps)
- Never use `npx prisma generate` directly - use the npm script

### DAG Workflow Engine
- Requirements: docs/DAG_WORKFLOW_ENGINE.md
- Types: docs/GRAPH_TYPES.md
- Expression evaluator namespaces: ctx.*, doc.*, segment.*
- 12 registered activity types

### Benchmarking System
- Evaluators: apps/temporal/src/evaluators/ (schema-aware, black-box)
- Evaluator types duplicated in apps/temporal/src/benchmark-types.ts
- Aggregation module: apps/temporal/src/benchmark-aggregation.ts
- Temporal activities for benchmarks in apps/temporal/src/activities/benchmark-*.ts

## Iteration Log
(Learnings from each iteration will be appended below)

## 2026-02-15 - US-036
- What was implemented: Side-by-side run comparison UI
- Files changed:
  - apps/frontend/src/features/benchmarking/pages/RunComparisonPage.tsx (new)
  - apps/frontend/src/features/benchmarking/pages/ProjectDetailPage.tsx
  - apps/frontend/src/features/benchmarking/pages/index.ts
  - apps/frontend/src/App.tsx
  - apps/frontend/src/features/benchmarking/hooks/useDefinitions.ts
- **Learnings for future iterations:**
  - Multiple useRun hooks can be called in a loop to fetch multiple runs in parallel
  - React Router `useSearchParams` is ideal for comma-separated query params like `?runs=id1,id2`
  - Checkbox selection pattern: store selected IDs in state, toggle on checkbox change, prevent propagation to avoid triggering row click
  - When using multiple instances of the same interface across components, ensure all required fields are synchronized (e.g., scheduleEnabled field was missing)
  - Client-side export: create Blob -> URL.createObjectURL -> anchor click -> URL.revokeObjectURL
---
